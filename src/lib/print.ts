import { ServiceOrder } from '../types/orders';

export function printOrder() {
  // La impresión ahora es manejada por @react-thermal-printer
  // Esta función se mantiene por compatibilidad
  window.print();
}